<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Payment Cancelled</title>
    <style>
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      body {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        font-family: 'Roboto', sans-serif;
        background-color: #fce4ec; 
        animation: fadeIn 1s ease-in-out;
      }

      @keyframes fadeIn {
        0% {
          opacity: 0;
        }
        100% {
          opacity: 1;
        }
      }

      .container {
        text-align: center;
        padding: 2rem;
        background: #ffffff;
        border-radius: 15px;
        box-shadow: 0 6px 15px rgba(255, 0, 0, 0.2);
        max-width: 500px;
        width: 90%;
        animation: slideUp 0.8s ease-out;
      }

      @keyframes slideUp {
        0% {
          transform: translateY(20px);
          opacity: 0;
        }
        100% {
          transform: translateY(0);
          opacity: 1;
        }
      }

      .cancel-icon {
        font-size: 5rem;
        color: #d32f2f; /* Dark red for error icon */
        margin-bottom: 1rem;
        animation: pulse 1.5s infinite;
      }

      @keyframes pulse {
        0% {
          transform: scale(1);
        }
        50% {
          transform: scale(1.1);
        }
        100% {
          transform: scale(1);
        }
      }

      h1 {
        color: #c62828; /* Darker red for the heading */
        font-size: 1.8rem;
        margin-bottom: 1rem;
      }

      p {
        color: #b71c1c; /* Medium red for text */
        font-size: 1rem;
        margin-bottom: 2rem;
      }

      .btn-home {
        display: inline-block;
        padding: 1rem 2rem;
        background-color: #e53935;
        color: #fff;
        text-decoration: none;
        border-radius: 30px;
        font-size: 1rem;
        transition: all 0.3s ease;
      }

      .btn-home:hover {
        background-color: #c62828;
        transform: translateY(-3px);
        box-shadow: 0 4px 12px rgba(0, 128, 0, 0.3);
      }

      @media (max-width: 480px) {
        .container {
          padding: 0.5rem 1.5rem 2.5rem 1.5rem;
        }

        .cancel-icon {
          font-size: 4rem;
        }

        h1 {
          font-size: 1.4rem;
        }

        p {
          font-size: 0.9rem;
        }

        .btn-home {
          padding: 0.85rem 1.5rem;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="cancel-icon">❌</div>
      <h1>Payment Cancelled</h1>
      <p>Your payment has been cancelled.</p>
      <a href="/" class="btn-home">Return to Home</a>
    </div>
  </body>
</html>
